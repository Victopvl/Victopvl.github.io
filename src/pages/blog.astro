---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

// Clonamos y ordenamos las publicaciones desde la más reciente
const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<BaseLayout title="Feed de Actualizaciones | Victoria Vallejos" sideBarActiveItemID="blog">
  <div class="pb-6 mt-5">
    <h1 class="text-3xl w-full font-bold text-base-content">Feed de Actualizaciones</h1>
    <p class="text-base text-base-content/70 mt-2">
      Bitácora en tiempo real sobre proyectos, hackatones, aprendizajes técnicos y el "Build in Public" de Datavolve.
    </p>
  </div>

  {
    posts.length === 0 ? (
      <div class="bg-base-200 border border-neutral/10 rounded-xl p-8 text-center mt-6 shadow-sm">
        <p class="text-base-content/70 text-sm italic">Aún no hay publicaciones en tu feed. ¡Es momento de lanzar la primera!</p>
      </div>
    ) : (
      <div class="space-y-6 mt-6 max-w-3xl">
        {posts.map((post) => (
          <div class="border border-neutral/10 bg-base-200 rounded-xl p-6 shadow-sm hover:shadow-md transition-all flex flex-col justify-between">
            <div>
              <div class="flex items-center gap-3 mb-3">
                <span class="text-[10px] uppercase font-extrabold tracking-wider px-2.5 py-1 rounded bg-primary/10 text-primary">
                  {post.data.badge || "Actualización"}
                </span>
                <span class="text-xs text-base-content/60 font-medium">
                  {post.data.pubDate.toLocaleDateString("es-CL", {
                    year: "numeric",
                    month: "short",
                    day: "numeric",
                  })}
                </span>
              </div>

              <h3 class="text-xl font-bold mb-2 text-base-content hover:text-primary transition-colors leading-snug">
                <a href={`/blog/${post.slug}/`}>{post.data.title}</a>
              </h3>

              <p class="text-sm text-base-content/80 leading-relaxed mb-4">
                {post.data.description}
              </p>
            </div>

            <div class="flex items-center justify-between flex-wrap gap-3 pt-3 border-t border-neutral/10 mt-2">
              <div class="flex flex-wrap gap-1.5">
                {post.data.tags &&
                  post.data.tags.map((tag) => (
                    <span class="text-xs px-2.5 py-0.5 rounded-full bg-base-100 border border-neutral/5 text-base-content/70 font-medium">
                      #{tag}
                    </span>
                  ))}
              </div>
              <a
                href={`/blog/${post.slug}/`}
                class="text-xs font-semibold text-primary hover:text-primary/80 transition-colors flex items-center gap-1 group"
              >
                Ver publicación completa <span class="transform group-hover:translate-x-0.5 transition-transform">→</span>
              </a>
            </div>
          </div>
        ))}
      </div>
    )
  }
</BaseLayout>